

$(document).ready(function() {

    $(document).on("click",function(){

       // $('.square').transition({ x: '100px' });
       // $('.square').transition({ y: '100px' });
       // $('.square').transition({ x: '40px', y: '40px' });

        // TRANFORM
       // $('.square').transition({ opacity: 0 });
       // $('.square').transition({ marginTop: '10px', height: '50px' });

        // 2D transformations
       // $('.square').transition({ rotate: '80deg' });
       // $('.square').transition({ scale: 2.2 });
       // $('.square').transition({ skewX: '30deg' });

        // 3D transformations
        /*$('.square').transition({
            perspective: '100px',
            rotateX: '180deg'
        });*/

       /* $('.square').transition({
            perspective: '100px',
            rotateY: '180deg'
        });*/


    });



});